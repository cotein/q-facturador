export const SearchNumberGetter = (state) => state.search_number;

export const PersonGetter = (state) => state.person;

export const PersonIsArrayGetter = (state) => state.personIsArray;

export const PersonFoundNameGetter = (state) => state.personFound.name;

export const PersonFoundLastNameGetter = (state) => state.personFound.last_name;

export const PersonFoundNumberGetter = (state) => state.personFound.number;

export const PersonAfipInscriptionIdGetter = (state) => state.personFound.afip_inscription_id;

export const PersonAfipInscriptionNameGetter = (state) => state.personFound.afip_inscription_name;

export const PersonDocumentTypeIdGetter = (state) => state.personFound.document_type_id;

export const PersonDocumentTypeNameGetter = (state) => state.personFound.document_type_name;